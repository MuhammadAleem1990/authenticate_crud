<!DOCTYPE html>
<html>
<body>
<% if @students.blank? %>
<p>There are not any students currently in the system.</p>
<% else %>
<p>These are the students list</p>
<ul id = "students">
<% @students.each do |c| %>
<li>
<%= link_to c.first_name, {:action => 'show', :id => c.id} -%>
<b> <%= link_to 'Edit', {:action => 'edit', :id => c.id} %></b>
<b> <%= link_to "Delete", {:action => 'delete', :id => c.id},
:confirm => "Are you sure you want to delete this item?" %></b>
</li>
<% end %>
</ul>

<% end %>
<p><%= link_to "Add new Student", students_new_path %></p>
<%= button_to 'Logout', logout_path, method: :destroy %>
</body>

</html>